# News Credibility Tracker — 使用說明

> 最後更新：**2026-05-31**（Week 3 完成）
> 公開網址：<https://news-credibility.vercel.app>
> 原始碼：<https://github.com/chengfengguo727-cmd/news-credibility>

這份是**操作手冊**——告訴你怎麼打開、怎麼看、各欄位什麼意思、怎麼維持資料新鮮。
如果你是要動程式碼，看 [`README.md`](README.md)。

---

## 目錄

1. [這是什麼](#這是什麼)
2. [打開應用](#打開應用)
3. [Dashboard 各區塊與欄位](#dashboard-各區塊與欄位)
4. [Ticker 鑽取頁](#ticker-鑽取頁)
5. [本機 Pipeline 怎麼跑](#本機-pipeline-怎麼跑)
6. [排程設定（Windows）](#排程設定windows)
7. [監控與維護](#監控與維護)
8. [常見問題 FAQ](#常見問題-faq)
9. [專案地圖](#專案地圖)
10. [更新紀錄](#更新紀錄)

---

## 這是什麼

一個追蹤財經媒體**前瞻性發言準確度**的系統。流程：
1. 自動每小時抓 CNBC + Yahoo Finance 的 RSS（GitHub Actions cron）
2. 用 Claude Haiku 4.5 抽出**可驗證的預測**（分析師目標價、宏觀預測、個股事件）+ **文章 metadata**（情緒、提到的 ticker、板塊、敘事標籤）
3. 等預測到期，用 yfinance（美股）/ FRED（宏觀）對到實際市場數據
4. 累積 hit/miss 統計，最終算出每家媒體的「信用度分數」

目前狀態：**MVP**——個人用、未對外行銷，資料庫累積中。

---

## 打開應用

### 主 dashboard

直接開瀏覽器：<https://news-credibility.vercel.app>

完全公開（不需登入），每分鐘自動更新一次資料。

### Ticker 鑽取

任何 ticker 點下去 → `/ticker/{symbol}`，例如：
- <https://news-credibility.vercel.app/ticker/NVDA>
- <https://news-credibility.vercel.app/ticker/2330.TW>（台積電）

### Supabase 後台（看原始資料）

<https://supabase.com/dashboard> → 你的專案 → **Table Editor**
查 SQL 用 **SQL Editor**。常用查詢都列在 README.md 的 "Useful queries" 區。

---

## Dashboard 各區塊與欄位

### 🔥 Trending tickers (7d)

過去 7 天被新聞提到的股票排行。

| 欄位 | 意思 |
|---|---|
| **Ticker** | 股票代號（美股大寫；台股 `xxxx.TW`；港股 `xxxx.HK`）。點下去到鑽取頁 |
| **Sentiment mix** | 一條彩色長條，比例代表這檔股票被「看多 / 中性 / 混和 / 看空」的文章佔比。🟢 綠 / ⚫ 灰 / 🟡 黃 / 🔴 紅 |
| **Bull / Bear** | 看多文章數 / 看空文章數（同情緒 mix 的數字，方便比對） |
| **Primary** | 這檔股票**作為文章主角**被提的次數（vs 順便被提到的）。Primary 數高 = 焦點公司 |
| **Total** | 7 天總提及次數 |

**怎麼看**：
- Total 高 = 媒體在講
- Bull / Bear 比例極端（例如 ZS 7:0 都空、MU 6:0 都多） = 明確的方向性敘事
- Primary / Total 比例高 = 這檔是討論主角，不是配角
- 不會顯示指數（`^GSPC` 等以 `^` 開頭的自動過濾）

### 📈 Mention growth — 24h vs 7-day baseline

「**最近被提及次數成長率最高的公司**」——你之前提的那個需求。

| 欄位 | 意思 |
|---|---|
| **Ticker** | 股票代號 |
| **24h mentions** | 過去 24 小時被提的次數 |
| **7d daily avg** | 過去 7 天「每日平均提及」（**排除最近 24h**，純當基準線） |
| **Growth** | `24h ÷ 7d_avg` 的倍數。**3.0× 表示今天熱度是平日 3 倍** |

特殊標籤：
- 🆕 **NEW** badge：7 天內從未被提過、現在突然出現。可能是新公司、IPO、突發事件
- **粗體綠色 2.0×+**：熱度顯著上升

> 篩選門檻：24h 內 ≥ 3 次提及才上榜（避免單篇噪音）。
> view 是空的代表 24h 內沒新文章——通常是你還沒跑當日 ingest，或者剛凌晨。

### 🏷️ Trending narratives (7d)

過去 7 天最常被提到的**市場敘事**標籤。

由 Claude 從每篇文章抽出 0-5 個 `snake_case` 標籤。沒有固定清單，常見如：

| 標籤 | 意思 |
|---|---|
| `ai_demand` | AI 需求面（GPU、雲、應用）|
| `ai_capex` | AI 資本支出（hyperscaler 投資）|
| `rate_cuts` | 降息預期 |
| `soft_landing` | 軟著陸 |
| `china_slowdown` | 中國經濟放緩 |
| `inflation_sticky` | 通膨黏滯 |
| `oil_supply_shock` | 油價供給衝擊 |
| `geopolitical_risk` | 地緣風險 |
| `earnings_season` | 財報季 |
| `regulatory_crackdown` | 監管打壓 |

**怎麼看**：哪些故事正在加熱。新標籤突然出現代表新議題在發酵。

### 🏢 Hot sectors (7d)

過去 7 天最熱的**板塊**。控制詞彙約 30 個（半導體、銀行、能源、雲、AI、零售、生技…）。Claude 自由標 1-5 個/文章。

**怎麼看**：板塊輪動信號。如果 `energy` 突然上來，可能伴隨油價或地緣事件。

### 📊 Per-source coverage

每家媒體的**抽取統計**。

| 欄位 | 意思 |
|---|---|
| **Source** | 媒體 id（`cnbc` / `yahoo_finance`） |
| **Articles** | 累積抓到的文章總數 |
| **With meta** | 已經跑過 Claude metadata 抽取的數量（< Articles 代表還在 backfill） |
| **Quality** | 模型給的**平均原創度**（0-1，1=有採訪/數據；0=bot 改稿） |
| **Claims** | 抽出的可驗證預測總數 |
| **Claims/article** | 平均每篇有幾個 claim。**越高越「肯說話」的媒體** |

**對你的判讀**：
- 兩家 quality 差很多 = 一家做原創、一家轉發
- Claims/article 差很多 = 一家點評多、一家純報導

> 已知：Reuters 已停用（直連 RSS 全 401，唯一可用的 Google News 代理只給標題、quality 低）。
> 之後要加回 Reuters 需要 NewsAPI 付費 tier 或 GDELT。

### 🗞️ Recently ingested

最近 10 篇剛抓進來的新聞，連結直接點到原文。新文章還沒抽 meta 前也會顯示——確認 ingest cron 正常運作。

---

## Ticker 鑽取頁

點任何 ticker → 進入該股票的歷史視圖。例：<https://news-credibility.vercel.app/ticker/NVDA>

### 標頭

```
NVDA  [+8 net sentiment]
47 mentions over the last 30 days · 12 bullish · 4 bearish · 28 neutral · 3 mixed
```

| 元件 | 意思 |
|---|---|
| **Ticker 大字** | 你查的代號 |
| **net sentiment badge** | `bull − bear`。正數綠色（多）、負數紅色（空）、0 灰色（中性） |
| **mentions over 30 days** | 30 天累計提及 |

### Daily mentions bar chart

30 天每日提及量，**用情緒疊色**：
- 🟢 綠 = 看多
- ⚫ 灰 = 中性
- 🟡 黃 = 混和
- 🔴 紅 = 看空

把滑鼠停在 bar 上會出現 tooltip：`2026-05-30 — 5 mentions (3↑ 0↓ 2· 0~)`

### Recent articles 列表

| 元素 | 意思 |
|---|---|
| 左側**色點** | 該文章對這檔的情緒（綠/灰/黃/紅）|
| **primary 標籤** | 文章主角就是這檔（不是配角） |
| 標題 | 點擊開原文 |
| 媒體 + 時間 | 灰色小字 |

---

## 本機 Pipeline 怎麼跑

完整 setup 步驟在 `README.md`。**已經 setup 過**的話日常操作：

### 開 PowerShell + 啟用 venv（每次重開視窗都要）

```powershell
cd D:\claude_code\news-credibility\backend
.\.venv\Scripts\Activate.ps1
```

prompt 前面出現 `(.venv)` 代表 OK。

### 三個指令

```powershell
# 1. 抓最新 RSS（GitHub Actions 已自動每小時跑，本機通常不用手動跑）
python -m tasks.run ingest

# 2. 把未處理的文章送 Claude 抽 claim + metadata
#    --limit N 控制單次處理量。預設 50。會吃 Claude 訂閱額度。
python -m tasks.run extract --limit 50

# 3. 把過期的 claim 對到實際市場數據（hit / miss / partial）
#    無外部成本（yfinance + FRED 都免費）。
python -m tasks.run validate
```

### 看執行結果

每個指令跑完最後一行就是統計：

```
# ingest:
reuters: fetched=0 inserted=0 skipped=0 errors=0    # Reuters 停用，這行會是 0
cnbc: fetched=60 inserted=12 skipped=48 errors=0
yahoo_finance: fetched=42 inserted=8 skipped=34 errors=0

# extract:
articles=50 claims=4 tickers=72 sectors=58 tags=42 errors=2 cost_usd=0.4123

# validate:
due=3 resolved=3 hit=2 partial=1 miss=0 pending=0
```

`errors` 高代表撞到 rate limit；隔幾小時再跑。

---

## 排程設定（Windows）

不想每天手動跑 extract？用 Windows Task Scheduler 自動排程。

### 安裝排程

```powershell
cd D:\claude_code\news-credibility\backend
.\setup_scheduler.ps1
```

看到綠色框框就是裝好了。2 分鐘後第一次自動跑。

### 參數變體

```powershell
.\setup_scheduler.ps1 -Limit 100              # 一次跑 100 篇
.\setup_scheduler.ps1 -IntervalMinutes 30     # 每 30 分鐘
.\setup_scheduler.ps1 -Limit 30 -IntervalMinutes 30   # 保守版（避 rate limit）
.\setup_scheduler.ps1 -Uninstall              # 移除排程
```

重跑同一個指令直接覆蓋舊任務（idempotent），不會疊兩份。

### 排程的「為什麼」

| 設計 | 為什麼 |
|---|---|
| **只在你登入時跑** | Claude Code OAuth 在你的 user profile 下；登出就沒 token 可用 |
| **電池供電也照跑** | 出差時也能用 |
| **電腦睡眠時錯過會補跑** | 醒來會立刻補一次（不會兩天都沒抽） |
| **同時只跑一個** | 上次還沒結束就 skip，避免疊加吃額度 |
| **單次最多 2 小時** | 防止 hang |
| **連續 15 個 error 就 early-stop** | 撞到 rate limit 不再硬幹，等下個 cycle |

### 暫停（不刪除）

```powershell
Disable-ScheduledTask -TaskName NewsCredibility-Extract  # 出國一週用
Enable-ScheduledTask  -TaskName NewsCredibility-Extract  # 回來再開
```

---

## 監控與維護

### 查排程狀態

```powershell
# 看下次什麼時候跑、上次成不成功
Get-ScheduledTaskInfo -TaskName NewsCredibility-Extract

# 手動立刻觸發一次
Start-ScheduledTask -TaskName NewsCredibility-Extract
```

### 即時看 log

```powershell
Get-Content D:\claude_code\news-credibility\backend\extract_scheduled.log -Tail 30 -Wait
```

Ctrl+C 退出。每次排程跑都會留：
```
[2026-05-31 14:00:01] === extract --limit 50 START ===
... per-article logs ...
articles=50 claims=4 tickers=72 sectors=58 tags=42 errors=2 cost_usd=0.4123
[2026-05-31 14:08:43] === END (exit 0) ===
```

### 查 DB 進度

開 Supabase Dashboard → SQL Editor → 跑：

```sql
-- 還剩多少要 backfill
SELECT
  COUNT(*) FILTER (WHERE body IS NOT NULL) AS with_body_total,
  COUNT(*) FILTER (WHERE body IS NOT NULL
                  AND id IN (SELECT article_id FROM article_meta)) AS done,
  COUNT(*) FILTER (WHERE body IS NOT NULL
                  AND id NOT IN (SELECT article_id FROM article_meta)) AS pending
FROM articles;

-- 已 resolved 的 claim 情況
SELECT outcome, COUNT(*) FROM claim_outcomes GROUP BY outcome;

-- 每家 source 的覆蓋進度
SELECT source,
       COUNT(*) AS total,
       COUNT(m.article_id) AS extracted,
       ROUND(100.0 * COUNT(m.article_id) / COUNT(*), 1) AS pct_done
FROM articles a LEFT JOIN article_meta m ON m.article_id = a.id
GROUP BY source;
```

---

## 常見問題 FAQ

### Q1. Dashboard 出現「No data yet」

可能原因：
1. Backfill 還沒跑完（meta 表是空的）→ 手動跑 `python -m tasks.run extract --limit 50` 幾次
2. View 沒套用 → README 的 setup 步驟 2 重跑一次 `schema.sql`
3. Vercel 環境變數沒設 → Vercel Dashboard → Settings → Environment Variables 檢查 `NEXT_PUBLIC_SUPABASE_URL` 跟 `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`

### Q2. `python -m tasks.run extract` 出現 `Got unexpected extra argument`

`typer` 版本太舊（< 0.15）跟新 `click` 不相容。升級：

```powershell
pip install --upgrade "typer>=0.15.1"
```

### Q3. `claude exited 1:` 大量出現

撞到 Claude Pro/Max 訂閱 rate limit（5 小時滾動窗口）。處理：
- 等 1-3 小時讓額度回血
- 程式碼已內建：連續 15 個 error 自動 early-stop，不會白燒
- 如果想 24/7 大量跑 → 改用 API key 走 Anthropic SDK 路線（要付錢，rate limit 高很多）

### Q4. extract 一篇成本是不是太高？

| 情境 | 預期 cost |
|---|---|
| 該 5 分鐘窗口第一篇（cache miss） | ~$0.05-0.06 |
| 同窗口後續（cache hit） | ~$0.005-0.01 |
| 連續 batch 50 篇 | ~$0.50-1.00 |

如果單篇看到 $0.10+，看一下 article body 字數——可能是異常長的文章。

### Q5. Reuters 的數字看起來怪

**Reuters 已停用**。歷史上有 97 筆透過 Google News 抓的 headline-only 資料，quality 0.26（filler 等級），不參考。不會新增。要看 Reuters 之後可能要走 NewsAPI 付費 tier。

### Q6. claims 都 pending 沒有 resolved

大多 analyst_target 是 **12 個月後到期**，現在 deadline 還沒過很正常。`validate` 只會處理過期的 claim。每天跑一次 validate，等時間到自然會解。

### Q7. ticker 頁面顯示空白

該 ticker 在我們語料裡從沒被提過，或者拼錯了。試 `NVDA` / `AAPL` / `MU` / `MSFT` 這種高曝光的。

### Q8. Vercel deploy 失敗

99% 是 `Root Directory` 沒設成 `frontend`。到 Vercel Dashboard → 專案 → Settings → General → 把 Root Directory 改成 `frontend`，然後 Redeploy。

### Q9. 想看詳細 SQL

[README.md 的 "Useful queries" 區](README.md#useful-queries)有複製即用的常用查詢。

---

## 專案地圖

```
news-credibility/
├── 使用說明.md             ← 你正在看
├── README.md               ← 給開發者的 setup / API 參考
├── backend/                ← Python pipeline
│   ├── ingest/             ← RSS 抓取
│   ├── extract/            ← Claude claim/meta 抽取
│   ├── validate/           ← yfinance + FRED 對證
│   ├── score/              ← Bayesian 計分（Week 4+ 用）
│   ├── tasks/run.py        ← CLI: ingest / extract / validate
│   ├── db/schema.sql       ← Postgres schema（idempotent）
│   ├── setup_scheduler.ps1 ← Windows 排程安裝器
│   └── tests/              ← pytest
├── frontend/               ← Next.js dashboard
│   ├── app/page.tsx        ← 首頁
│   ├── app/ticker/[symbol] ← 鑽取頁
│   └── lib/queries.ts      ← 所有 Supabase 查詢
└── .github/workflows/      ← 每小時 ingest cron
    └── ingest.yml
```

---

## 更新紀錄

| 日期 | 變更 |
|---|---|
| **2026-05-31** | Week 3：yfinance + FRED 接通；validate 命令；新增 Ticker 鑽取頁；setup_scheduler.ps1；本說明書建立 |
| 2026-05-28 | Week 2.5：article metadata（情緒、提到的 ticker、板塊、敘事標籤）；4 個 dashboard view；Reuters 停用 |
| 2026-05-22 | Week 2：claim_extractor 改用 `claude` CLI（OAuth）；本機 only；Anthropic SDK 不再使用 |
| 2026-05-21 | Week 1：RSS ingest + schema + Bayesian 計分骨架 + Next.js 上 Vercel + GitHub Actions cron |

### 何時更新本說明書

每次新增以下任一項都要回來改：
- 新 dashboard 區塊或欄位
- 新 CLI 指令
- 新 schema 表 / view
- 新外部依賴（CLI 工具、API key）
- 行為改變（hit threshold、cache TTL、預設 limit）
- 新 FAQ entry（讀者真的問了的問題）

格式：在「更新紀錄」加一行 + 修改受影響的章節 + 更新檔頭日期。
